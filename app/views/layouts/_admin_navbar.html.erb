<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% helpful_list_links.each do |text, target| %>
          <li class=<%= (text == yield(:title)) ? 'active' : '' %>><%= link_to text, target %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-left">
        <%= link_to t('videos.new'), new_video_path, :class => 'btn btn-primary navbar-btn navbar-right' %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">خيارات المدير <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li class=<%= (current_page? dashboard_path) ? 'active' : '' %>>
              <%= link_to t('users.dashboard'), dashboard_path %>
            </li>
            <li class="disabled"><a href="#"><%= t('users.manage_users') %></a></li>
            <li role="separator" class="divider"></li>
            <li>
              <%= link_to logout_path, method: :delete do %>
                <%= t('users.logout') %>
                <i class="fa fa-sign-out fa-rotate-180" aria-hidden="true"></i>
              <% end %>
            </li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
